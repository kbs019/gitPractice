<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ex.repository.QnaBoardMapper">	

  <!-- 전체 글 갯수 -->
  <select id="boardCount" resultType="int">
    select count(*) from qna_board
  </select>
  <!-- 글 목록 -->
  <select id="boardList" resultType="com.ex.gitprac.data.QnaBoardDTO">
    <@[CDATA[
    select * from ( select b.*, rownum r from ( select * from qna_board order by postNo desc ) b ) where r >= #{start} and r <= #{end}
    ]]>
  </select>

  <!-- 글 작성 -->
  <insert id="postInsert" resultType="int" parameterType="com.ex.gitprac.data.QnaBoardDTO">
    insert into qna_board values( qna_board_seq.nextval, #{writer}, #{title}, #{content}, #{imgName}, #{imgPath}, #{originalName}, #{showRecord}, sysdate, 0, 0, 0 )
  </insert>

  <!-- 조회수 + 1 -->
  <update id="viewsUp" parameterType="int">
    update qna_board set views = views + 1 where postNo = #{postNo}
  </update>
  <!-- 글 내용 -->
  <select id="postContent" resultType="com.ex.gitprac.data.QnaBoardDTO" parameterType="int">
    select * from qna_board where postNo = #{postNo}
  </select>
</mapper>